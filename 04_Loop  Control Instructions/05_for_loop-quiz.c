#include <stdio.h>

int main()
{
    int a = 1, n;
    printf("Enter the limite of integer number:\n");
        scanf("%d", &n);
    for (a; a <= n; a++)
    {
        printf("%d is the natural number\n",a);
    }
    return 0;
}