#include <stdio.h>

int main()
{
    int radius = 3;
    float pi = 3.1416;
    printf("The area of this circle is %f\n", pi * radius * radius);
    int height = 3;
    printf("Volume of this cylinder is %f", pi * radius * height);
    return 0;
}