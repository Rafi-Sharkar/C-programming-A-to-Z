#include<stdio.h>

int main()         // int main() is the function where from the execution start and at return 0; execution will terminate

{
    int _tom;       // variable is an entity whose value can be changed.
    int _Tom;       // case sencitive.

    int a = 3;      // a assign as 3


// single line comment

/*
this is multiline comment
most cases we are
going to use single line comment
*/
    printf("There  we want to learn about variable");       // printf is also a function whose work is to print. with a double comma ("sa") sa is string

    return 0;
}






// (;) is a instuction terminator
// Instuctions are executed in the same order in which they are written

// Type of constant_____

// integer constant --> -1, 6, 7, 9
// real constant --> 322.1, 2.5, 7.0
// character constant --> 'a', '$', '@' ( must be enclosed within single inverted commas)

// Keyworlds
// there have 32 keywords in C language which we can't use as a variable
