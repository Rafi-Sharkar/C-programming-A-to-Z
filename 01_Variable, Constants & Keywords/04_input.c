// boiler code plate
#include <stdio.h>
int main()
{
    int a, b;
    printf("Enter the value of a\n");
    scanf("%d", &a);       // %d ==> Format specifier;      &a ==> address of a; [ and which value we gonna be input it will go address of a]

    printf("Enter the value of a\n");
    scanf("%d", &a);       

    printf("the sum of a and b %d", a+b); 
}
